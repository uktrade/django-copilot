[tool.poetry]
name = "copilot-django"
version = "0.1.0"
description = "Helper functions for running django in copilot/ECS"
authors = ["DIT SRE <tools@digital.trade.gov.uk>"]
readme = "README.md"
packages = [{include = "copilot_django"}]

[tool.poetry.dependencies]
python = "^3.11"
requests = "^2.31.0"

[tool.poetry.group.dev.dependencies]
pytest = "^7.3.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
